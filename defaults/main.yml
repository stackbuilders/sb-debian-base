---
# defaults file for sb-base
deploy_user_id: 1000
deploy_group_id: 1000
deploy_platform: metal # valid value rackspace, aws, metal


authorized_keys:
  administrators: &administrators_ssh_keys
     - https://github.com/jsl.keys
     - https://github.com/lpaulmp.keys
  deployers:
     <<: *administrators_ssh_keys
